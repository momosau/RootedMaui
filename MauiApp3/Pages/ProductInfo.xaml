<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp3.Pages.ProductInfo"
             Title="ProductInfo"
             xmlns:ViewModel="clr-namespace:MauiApp3.ModelView" 
             x:DataType="ViewModel:ProductInfoVIewModel"
             BackgroundColor="#E7E9E4" 
             FlowDirection="RightToLeft" 
             Shell.NavBarIsVisible="False">

    <VerticalStackLayout>
        
        <Image Source="{Binding SelectedProduct.ImageUrl}"/>
    <Label Text="{Binding SelectedProduct.Name}" FontSize="Large" TextColor="Black"/>
    <Label Text="{Binding SelectedProduct.Weight}" FontSize="Medium" TextColor="Black"/>
    <Label Text="{Binding SelectedProduct.Unit}" FontSize="Medium" TextColor="Black"/>
    <Label Text="{Binding SelectedProduct.Price, StringFormat='{0} ريال'}" FontSize="Large" TextColor="Black"/>
            
        

        <HorizontalStackLayout Spacing="10" VerticalOptions="Center" HorizontalOptions="Start">
        <Button
        Text="-"
        Background="White"
        TextColor="Green"
        Clicked="DelButtonClicked"
        CornerRadius="100"
        WidthRequest="40"
        HeightRequest="40" 
    />

        <Label x:Name="label" Text="{Binding SelectedProduct.Quantity}" TextColor="Black" 
           HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />

        <Button
        Text="+"
      Background="White"
   TextColor="Green"
        Clicked="AddButtonClicked"
        CornerRadius="100"
        WidthRequest="40"
        HeightRequest="40" />
    </HorizontalStackLayout>
    </VerticalStackLayout>
</ContentPage>